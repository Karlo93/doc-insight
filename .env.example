POSTGRES_USER=di
POSTGRES_PASSWORD=di
POSTGRES_DB=di
POSTGRES_PORT=5432
DI_DATABASE_URL=postgresql+psycopg://di:di@localhost:5432/di

# Unset disables telemetry. OTLP/HTTP base URL; signal paths are appended.
# DI_OTEL_ENDPOINT=http://localhost:4318
# Unset uses the name passed to configure(service_name).
# DI_OTEL_SERVICE_NAME=worker
DI_ENV=development
